# Pipeline Dependencies
# Installation: pip install -r requirements.txt (use an isolated virtual environment).
# Prerequisite: A valid Java installation (JDK 8/11/17) is required for PySpark execution.

pyspark>=3.5,<3.6
pyyaml>=6.0
setuptools>=68  # Provides distutils shim for Python 3.12+ compatibility.

# Enable Delta Lake storage format (requires setting output_format: delta in config.yaml).
# delta-spark==3.2.0

# Model training, experiment tracking, and visualization dependencies
mlflow>=2.10
pandas>=2.0
seaborn>=0.13
notebook>=7.0

# Testing Framework.
pytest>=8.0

# Part C: Airflow for pipeline orchestration
apache-airflow==2.9.3