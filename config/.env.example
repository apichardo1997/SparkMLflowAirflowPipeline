# Optional environment overrides. Copy to .env and edit if needed.
# Defaults in config/config.yaml work without any of these.

# Path to an alternative config file.
# LAB3_CONFIG=config/config.yaml

# Override the MLflow tracking location (default: data/mlruns).
# MLFLOW_TRACKING_URI=file:./data/mlruns
